export type Details = {
    price: number;
    title: string;
    link: string;
    description: string;
    image: string;
    reviewCount: number;
    rating: number;
    options: Record<string, string[]>;
}
