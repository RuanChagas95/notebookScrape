export type Details = {
    price: string;
    title: string;
    link: string;
    description: string;
    image: string;
    reviewCount: string;
    rating: number;
    options: Record<string, string>;
}
